/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[{
            hostname: "gpnatftzuhczaxmkthzl.supabase.co",
            protocol: "https"
        }]
    }
};


export default nextConfig;
